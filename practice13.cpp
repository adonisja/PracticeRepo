#include <iostream>
#include <climits>
#include <array>
#include <iomanip>
#include <cmath>
using namespace std;

/*  
*/

#include <iostream>
using namespace std;

int main()
{
    const int SIZE = 3;
    int grade[SIZE], sum = 0;
    for (int i = 0; i < SIZE; i++)
    {
        do
        {
            cout << "Please enter grade " << i + 1 << ": ";
            cin >> grade[i];
        } while (grade[i] < 1 || grade[i] > 5);
        sum += grade[i];
    }
    for (i in SIZE)
    cout << "Sum of ";
    for 
    return 0;
}